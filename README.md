# Quick Start to manange repository using jenkins

1. Generate gpg key for repository sign
 - $ cd gpg
 - $ ./generate_gpg_key.sh {username} {password}

2. start docker container
 - $ docker-compose up -d


